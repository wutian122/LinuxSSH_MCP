"""
Linux SSH MCP 常量定义模块

本模块集中管理项目中使用的所有常量，避免硬编码魔法数字，提高代码可维护性。
"""
from __future__ import annotations

# ============================================================
# 文件传输相关常量
# ============================================================

# 默认分块大小（字节）
DEFAULT_CHUNK_SIZE: int = 8192

# 最小分块大小（字节）
MIN_CHUNK_SIZE: int = 1024

# 最大分块大小（字节）
MAX_CHUNK_SIZE: int = 1024 * 1024  # 1MB

# ============================================================
# 缓冲区相关常量
# ============================================================

# 读取缓冲区大小（字节）
READ_BUFFER_SIZE: int = 4096

# ============================================================
# 超时相关常量
# ============================================================

# 默认命令超时时间（秒）
DEFAULT_COMMAND_TIMEOUT_SECONDS: int = 30

# 默认连接超时时间（秒）
DEFAULT_CONNECT_TIMEOUT_SECONDS: int = 30

# 交互式命令读取超时（秒）
INTERACTIVE_READ_TIMEOUT: float = 0.01

# ============================================================
# 连接池相关常量
# ============================================================

# 每主机默认最大连接数
DEFAULT_PER_HOST_MAX_CONNECTIONS: int = 5

# 空闲连接默认TTL（秒）
DEFAULT_IDLE_CONNECTION_TTL_SECONDS: int = 300

# ============================================================
# 缓存相关常量
# ============================================================

# 默认缓存最大容量
DEFAULT_CACHE_MAXSIZE: int = 128

# 静态缓存默认TTL范围（秒）
DEFAULT_STATIC_TTL_MIN_SECONDS: int = 300
DEFAULT_STATIC_TTL_MAX_SECONDS: int = 3600

# 动态缓存默认TTL范围（秒）
DEFAULT_DYNAMIC_TTL_MIN_SECONDS: int = 30
DEFAULT_DYNAMIC_TTL_MAX_SECONDS: int = 120

# ============================================================
# Token优化相关常量
# ============================================================

# 默认最大Token数
DEFAULT_MAX_TOKENS: int = 800

# Token估算：每N个非CJK字符约等于1个Token
TOKEN_CHARS_PER_TOKEN: int = 4

# 截断后缀
DEFAULT_TRUNCATE_SUFFIX: str = "..."

# ============================================================
# SSH相关常量
# ============================================================

# 默认SSH端口
DEFAULT_SSH_PORT: int = 22

# 默认Shell
DEFAULT_SHELL: str = "/bin/bash"

# ============================================================
# 日志相关常量
# ============================================================

# 默认日志级别
DEFAULT_LOG_LEVEL: str = "INFO"

# 默认日志轮转大小
DEFAULT_LOG_ROTATION: str = "10 MB"

# 默认日志保留时间
DEFAULT_LOG_RETENTION: str = "30 days"

# ============================================================
# 分页相关常量
# ============================================================

# 默认页码
DEFAULT_PAGE: int = 1

# 默认每页大小
DEFAULT_PAGE_SIZE: int = 100

# 缓存信息显示的最大键数
CACHE_INFO_HEAD_SIZE: int = 50
